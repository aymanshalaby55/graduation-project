{
  "name": "models",
  "version": "1.0.0",
  "description": "Python API for human detection models",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "test": "pytest"
  },
  "devDependencies": {
    "pytest": "^7.3.1"
  }
}
